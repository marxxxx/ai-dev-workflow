# Work on ai-dev-workflow itself inside its own agent image.
# Tool stack only (harnesses + managed MCPs + GitHub CLI); no generated
# dev-cycle definitions and no ai-project.json are required or read.
services:
  ai-dev-workflow:
    image: ${AGENT_IMAGE:-marxx/ai-dev-workflow:latest}
    working_dir: /workspace
    init: true
    stdin_open: true
    tty: true
    shm_size: 1gb
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    # Root setup remaps dev and owns its home, then drops privileges.
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETUID
      - SETGID
    environment:
      AGENT_TOOLSTACK_ONLY: "1"
      PROJECT_ROOT: ${PROJECT_ROOT:?Set PROJECT_ROOT to this repo's absolute path}
      HOST_UID: ${HOST_UID:-1000}
      HOST_GID: ${HOST_GID:-1000}
      CONTEXT7_API_KEY: ${CONTEXT7_API_KEY:-}
    volumes:
      - type: bind
        source: ${PROJECT_ROOT:?Set PROJECT_ROOT to this repo's absolute path}
        target: /workspace
        bind:
          create_host_path: false
      - type: volume
        source: agent-home
        target: /home/dev
      # Container-only dependency tree for docker/tools (the launcher and its unit tests
      # resolve smol-toml / jsonc-parser from here). Masks the host install so a host-side
      # `npm ci` and a container-side one never overwrite each other. Run `npm ci` in
      # docker/tools inside the container before invoking those tests.
      - type: volume
        source: deps-docker-tools
        target: /workspace/docker/tools/node_modules
    command: ["bash"]

volumes:
  agent-home:
  deps-docker-tools:
